{
  "name": "the-legend",
  "version": "1.0.0",
  "description": "outil de capture",
  "bin":"./run",
  "private": true,
  "scripts": {
    "preinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "start": "run"
  },
  "author": "Blacky",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "argparse": "^1.0.10",
    "babel-polyfill": "^6.26.0",
    "phantom": "^6.0.3"
  }
}
